machine:
    python:
        version: 2.7.6

dependencies:
    override:
        - pup install --upgrade setuptools
        - pip install -e .
test:
    override:
        - pip install pytest==3.2.0 && python ./setup.py develop && make check-full
